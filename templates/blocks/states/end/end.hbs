self.states.add({{ value_name }}, function(name) {
    return new EndState(name, {
        {{#if opts}}
            {{#each opts}}
                {{ label }} : {{ value }},
            {{\each}}
        {{\if}}

        text: {{ value_text }},
        next: {{ value_next }}
    });
});